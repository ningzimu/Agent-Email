{
  "name": "@mailbox/cli",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "mailbox-dev": "bin/mailbox.js"
  },
  "dependencies": {
    "@mailbox/core": "workspace:*",
    "@mailbox/shared": "workspace:*",
    "@mailbox/workflows": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^12.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "execa": "^9.4.0",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "node -e \"console.log('no lint configured')\"",
    "format": "node -e \"console.log('no format configured')\""
  }
}
